import { FullnameDirective } from './fullname.directive';

describe('FullnameDirective', () => {
  it('should create an instance', () => {
    const directive = new FullnameDirective(null);
    expect(directive).toBeTruthy();
  });
});
