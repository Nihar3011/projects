import { ShowstudentDirective } from './showstudent.directive';

describe('ShowstudentDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowstudentDirective();
    expect(directive).toBeTruthy();
  });
});
