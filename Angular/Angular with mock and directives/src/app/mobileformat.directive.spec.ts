import { MobileformatDirective } from './mobileformat.directive';

describe('MobileformatDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileformatDirective(null);
    expect(directive).toBeTruthy();
  });
});
